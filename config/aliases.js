const { paths } = require('./constants');

module.exports = {
  '@assets': paths.assets,
  '@constants': paths.constants,
  '@components': paths.components,
  '@pages': paths.pages,
  '@utils': paths.utils,
  '@hooks': paths.hooks
};
